self.addEventListener('install', () => {
	// Dummy install event
});

self.addEventListener('activate', () => {
	// Dummy activate event
});

self.addEventListener('fetch', () => {
	// Dummy fetch event
});
